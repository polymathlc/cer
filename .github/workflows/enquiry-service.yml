name: Public enquiry service
on:
  pull_request:
    paths: ['enquiry-function/**', 'tools/enquiry-rules.mjs', 'tools/enquiry-rules-tests.mjs', 'tools/enquiry-deploy-setup.mjs', 'tools/enquiry-smoke.mjs', 'tools/enquiry-deploy-tests.mjs', 'enquiry-core.mjs', '.github/workflows/enquiry-service.yml', '.github/workflows/deploy-enquiry.yml']
  push:
    branches: [main]
    paths: ['enquiry-function/**', 'tools/enquiry-rules.mjs', 'tools/enquiry-rules-tests.mjs', 'tools/enquiry-deploy-setup.mjs', 'tools/enquiry-smoke.mjs', 'tools/enquiry-deploy-tests.mjs', 'enquiry-core.mjs', '.github/workflows/enquiry-service.yml', '.github/workflows/deploy-enquiry.yml']
permissions:
  contents: read
jobs:
  enquiry-service:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: enquiry-function/functions/package-lock.json
      - run: npm ci --prefix enquiry-function/functions
      - run: npm test --prefix enquiry-function/functions
      - run: node --test tools/enquiry-rules-tests.mjs
      - run: node --check enquiry-function/functions/index.js
      - run: node tools/enquiry-deploy-tests.mjs
